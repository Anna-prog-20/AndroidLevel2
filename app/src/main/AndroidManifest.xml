<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidlevel2_lesson1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".App"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".data.ServiceWeather"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".town.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".weather.ActivityWeather" />
        <activity
            android:name=".historyWeather.ActivityHistoryWeather"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".info.ActivityInfo"
            android:label="@string/itemInfo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".setting.ActivitySettings"
            android:noHistory="true" />
    </application>

</manifest>